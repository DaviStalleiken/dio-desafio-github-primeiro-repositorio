/*

var jogadorUm = 0;
var jogadorDois = 0;
var placar;

//IF TERNÁRIO
jogadorUm != -1 && jogadorDois != -1 ? console.log('Os dois jogadores são válidos') : console.log('Jogadores inválidos');

//IF PADRÃO
    if (jogadorUm > 0 && jogadorDois == 0) {
        console.log('Jogador Um marcou um ponto');
        placar = jogadorUm > jogadorDois;
    }
//ELSE IF
    else if (jogadorDois > 0 && jogadorUm == 0) {
        console.log('Jogador Dois marcou um ponto');
        placar = jogadorDois > jogadorUm;

    }
//ELSE
    else {
        console.log('Ninguém marcou ponto');
    }

//SWITCH
switch (placar) {
    case placar = jogadorUm > jogadorDois:
        console.log('Jogador Um ganhou');
        break;
    case placar = jogadorDois > jogadorUm:
        console.log('Jogador Dois ganhou');
        break;
    default:
        console.log('Ninguém ganhou');
}

let array = ['valor1', 'valor2', 'valor3', 'valor4'];
let object = {propriedade1: 'valor1', proriedade2: 'valor2', propriedade3: 'valor3'};

//FOR
for (let i = 0; i <array.length; i++) {
    console.log(indice);
}

//FOR/IN
for (let i in array) {
    console.log(i);
}

//COM OBJECT
for (i in object) {
    console.log(i);
}

//FOR/OF
for (i of array) {
    console.log(i);
}

//COM OBJECT / Não é recomendado usar com objetos, pois neste caso com uma string, por exemplo, imprimiria cada letra em uma linha diferente
for (i of object.propriedade1) {
    console.log(i);
}

*/

//WHILE
var a = 0;
while (a < 10) {
    a++;
    console.log(a);
}







